from enum import IntEnum, auto

class Destinations(IntEnum):
    home = auto()
    browse = auto()
    search = auto()
    add = auto()
    edit = auto()
    loanInfo = auto()
    bookInfo = auto()
    personInfo = auto()
