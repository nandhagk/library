from enum import Enum, auto

class Destinations(Enum):
    home = auto()
    browse = auto()
    search = auto()
    add = auto()
    edit = auto()
    loanInfo = auto()
    bookInfo = auto()
    personInfo = auto()
